import styled from "styled-components";

export default function Header() {
  return (
    <Wrapper>
      <nav className="nav">
        <h1>Menu Generating Operations Widget Program</h1>
      </nav>
    </Wrapper>
  );
}

const Wrapper = styled.header``;
